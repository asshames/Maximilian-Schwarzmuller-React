import React from 'react';

const drawerToggle = (props) => (
    <div  onClick={props.clicked}>
        <div>Menu</div>
        <div></div>
        <div></div>
    </div>
);

export default drawerToggle;